[tool.ruff]
line-length = 100
target-version = "py312"
extend-exclude = ["alembic/versions"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "N"]
ignore = ["E501"]  # line too long — Pydantic/SQLAlchemy ont des lignes longues

[tool.ruff.format]
quote-style = "double"
